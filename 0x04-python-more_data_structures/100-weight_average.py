#!/usr/bin/python3
def weight_average(my_list=[]):
    if not my_list:
        return 0

    avrg = 0
    divder = 0

    for tup in my_list:
        avrg += avg[0] * avg[1]
        divder += avg[1]
    return float(avrg / divder)
